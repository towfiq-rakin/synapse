services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: synapse:latest
    container_name: synapse-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
